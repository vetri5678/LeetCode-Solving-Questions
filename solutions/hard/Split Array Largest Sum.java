// Title: Split Array Largest Sum
            // Difficulty: Hard
            // Language: Java
            // Link: https://leetcode.com/problems/split-array-largest-sum/

            sum=num;
        }
        else{
            sum+=num;
        }
       }
            parts++;
        if(sum+num>maxsum){
       for(int num:nums){
       int  sum=0;
       int parts=1;
    public boolean canpart(int nums[],int k,int maxsum){
    }
        return left;
        }
